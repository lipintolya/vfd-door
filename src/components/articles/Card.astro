---
interface Props {
  title: string
  image?: string
  imageAlt?: string
  tags?: string   // через запятую
}
const { title, image, imageAlt, tags } = Astro.props
const tagList = tags ? tags.split(',').map((t) => t.trim()).filter(Boolean) : []
---

<article class="dcard">
  <div class="dcard__media">
    {image ? (
      <img src={image} alt={imageAlt ?? title} loading="lazy" decoding="async" />
    ) : (
      <div class="dcard__ph" aria-hidden="true">
        <svg viewBox="0 0 24 24" fill="none">
          <rect x="4" y="3" width="13" height="18" rx="1.5" stroke="currentColor" stroke-width="1.4"/>
          <circle cx="13.5" cy="12" r="1" fill="currentColor"/>
          <path d="M17 21h3M20 3v18" stroke="currentColor" stroke-width="1.4" stroke-linecap="round"/>
        </svg>
        <span>Фото скоро</span>
      </div>
    )}
  </div>

  <div class="dcard__body">
    <h3 class="dcard__title">{title}</h3>
    {tagList.length > 0 && (
      <ul class="dcard__tags">
        {tagList.map((t) => <li>{t}</li>)}
      </ul>
    )}
    <div class="dcard__text"><slot /></div>
  </div>
</article>

<style>
.dcard {
  display: flex;
  flex-direction: column;
  border: 1px solid #e5e7eb;
  border-radius: 1rem;
  overflow: hidden;
  background: #fff;
  transition: border-color 200ms ease, box-shadow 200ms ease, transform 200ms ease;
}
.dcard:hover {
  border-color: #cbd5e1;
  box-shadow: 0 10px 30px rgba(15, 23, 42, 0.08);
  transform: translateY(-3px);
}

.dcard__media {
  aspect-ratio: 3 / 4;
  background: #f4f6f9;
  overflow: hidden;
}
.dcard__media img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
  display: block;
  margin: 0 !important;
  border-radius: 0 !important;
  transition: transform 400ms ease;
}
.dcard:hover .dcard__media img { transform: scale(1.04); }

.dcard__ph {
  width: 100%;
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  color: #cbd5e1;
  background: linear-gradient(135deg, #f8fafc, #eef2f7);
}
.dcard__ph svg { width: 2.25rem; height: 2.25rem; }
.dcard__ph span {
  font-size: 0.6875rem;
  font-weight: 600;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: #94a3b8;
}

.dcard__body {
  padding: 1.125rem 1.25rem 1.375rem;
  display: flex;
  flex-direction: column;
  gap: 0.625rem;
}
.dcard__title {
  margin: 0;
  font-size: 1.0625rem;
  font-weight: 500;
  letter-spacing: -0.01em;
  color: var(--color-ink, #0f172a);
}
.dcard__tags {
  list-style: none !important;
  margin: 0;
  padding: 0 !important;
  display: flex;
  flex-wrap: wrap;
  gap: 0.375rem;
}
.dcard__tags li::marker { content: ''; }
.dcard__tags li {
  list-style: none !important;
  margin: 0 !important;
  font-size: 0.75rem;
  font-weight: 600;
  color: #0d9488;
  background: #f0fdfa;
  border: 1px solid #99f6e4;
  padding: 0.2rem 0.6rem;
  border-radius: 9999px;
  white-space: nowrap;
}
.dcard__text {
  font-size: 0.9375rem;
  line-height: 1.6;
  color: #64748b;
}
.dcard__text :global(p) { margin: 0; }
.dcard__text :global(p + p) { margin-top: 0.5rem; }
</style>
