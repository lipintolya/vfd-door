# 📝 ИНСТРУКЦИЯ ПО FAVICON

## Что нужно создать

Для корректного отображения favicon на всех устройствах нужны следующие файлы в папке `public/`:

### Обязательные файлы:

1. **favicon.ico** (32x32px)
   - Для старых браузеров (IE, старые версии Chrome/Firefox)
   - Формат: `.ico`

2. **logo.svg** (любой размер)
   - Для современных браузеров (Chrome, Firefox, Safari)
   - Формат: `.svg`

3. **apple-touch-icon.png** (180x180px)
   - Для iOS устройств (iPhone, iPad)
   - Формат: `.png` с прозрачным фоном

4. **icon-192.png** (192x192px)
   - Для Android устройств и PWA
   - Формат: `.png`

5. **icon-512.png** (512x512px)
   - Для Android splash screen и PWA
   - Формат: `.png`

6. **safari-pinned-tab.svg** (любой размер)
   - Для Safari pinned tabs (macOS, iOS)
   - Формат: `.svg`

### Опционально (для социальных сетей):

7. **og-image.jpg** (1200x630px)
   - Для предпросмотра в соцсетях (VK, Telegram, WhatsApp)
   - Формат: `.jpg` или `.png`

---

## Где создать

### Вариант 1: Онлайн-генератор (рекомендую)

1. Откройте https://realfavicongenerator.net/
2. Загрузите ваш логотип (PNG 512x512)
3. Настройте отображение для каждой платформы
4. Скачайте архив
5. Распакуйте файлы в `public/`

### Вариант 2: Вручную

Если у вас есть логотип в SVG:

```bash
# Конвертация в ICO (нужен imagemagick)
convert -background none -density 384 logo.svg favicon.ico

# Конвертация в PNG
convert -background none -density 384 logo.svg -resize 192x192 icon-192.png
convert -background none -density 384 logo.svg -resize 512x512 icon-512.png
convert -background none -density 384 logo.svg -resize 180x180 apple-touch-icon.png
```

### Вариант 3: Заказать у дизайнера

Попросите сделать favicon pack в формате:
- SVG (основной)
- PNG 192x192
- PNG 512x512
- PNG 180x180 (Apple)
- ICO (фолбэк)

---

## Цвета

В `index.html` и `manifest.json` используется цвет `#0d9488` (teal-600).

Если ваш логотип другого цвета, замените:
- В `index.html`: `color="#0d9488"` и `content="#0d9488"`
- В `manifest.json`: `"theme_color": "#0d9488"`

---

## Проверка

После добавления файлов:

1. Очистите кэш браузера (Ctrl+Shift+R)
2. Проверьте в разных браузерах:
   - Chrome (Desktop)
   - Safari (macOS/iOS)
   - Firefox
   - Edge

3. Проверьте PWA:
   - Откройте Chrome DevTools → Application → Manifest
   - Все иконки должны отображаться

4. Проверьте соцсети:
   - https://developers.facebook.com/tools/debug/
   - https://www.linkedin.com/post-inspector/

---

## Временное решение

Пока нет готовых файлов, можно использовать:

1. Текущий `vite.svg` как временный favicon
2. Онлайн-генератор для быстрого создания

---

## Файлы для public/

```
public/
├── favicon.ico              ← создать
├── logo.svg                 ← создать (или переименовать vite.svg)
├── apple-touch-icon.png     ← создать
├── icon-192.png             ← создать
├── icon-512.png             ← создать
├── safari-pinned-tab.svg    ← создать
├── og-image.jpg             ← создать (для соцсетей)
├── manifest.json            ← ✅ уже создан
└── vite.svg                 ← можно удалить
```
